* Spring Boot application with Basic Auth, CRUD and required endpoints
* In resources, there is an importable postman collection to test the endpoints
* Unit tests cover whole flow through controller
